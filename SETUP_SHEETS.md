# 🔧 スプレッドシート連携セットアップ手順

このドキュメントでは、アプリをGoogleスプレッドシートと連携させる方法を説明します。

---

## 📋 ステップ1: スプレッドシートを作成

1. **新しいGoogleスプレッドシートを作成**
   - [Google Sheets](https://sheets.google.com) を開き、「空白」で新規作成

2. **シート名を変更**
   - 下のタブで「Sheet1」を右クリック → 名前を「**団員リスト**」に変更
   - 「＋」で新しいシートを追加 → 名前を「**訪問記録**」に変更

3. **「団員リスト」シートにヘッダーを追加**
   
   | A列 | B列 |
   |-----|-----|
   | id | name |
   | 101 | 田中太郎 |
   | 102 | 鈴木花子 |
   
   ※ 実際の団員番号・名前を入力してください

4. **「訪問記録」シートにヘッダーを追加**
   
   | A | B | C | D | E | F | G | H | I |
   |---|---|---|---|---|---|---|---|---|
   | id | shopName | city | status | note | lat | lng | castName | timestamp |

---

## 🚀 ステップ2: Apps Scriptをデプロイ

1. **スプレッドシートのメニューから「拡張機能」→「Apps Script」を開く**

2. **コードを貼り付け**
   - 既存のコード (`function myFunction...`) を全て削除
   - `blue-bird-tracker/google_apps_script.js` の中身を全てコピー＆ペースト

3. **保存**
   - Ctrl+S または 💾アイコン

4. **デプロイ**
   - 右上の「デプロイ」→「新しいデプロイ」
   - 種類: 「ウェブアプリ」を選択
   - 説明: 「Blue Bird API」など任意
   - 次のユーザーとして実行: 「自分」
   - アクセスできるユーザー: **「全員」**（重要！）
   - 「デプロイ」をクリック

5. **URLをコピー**
   - デプロイ後に表示されるURLをコピー
   - 例: `https://script.google.com/macros/s/XXXXXXX/exec`

---

## ⚙️ ステップ3: アプリに設定

1. **config.jsを編集**
   - `blue-bird-tracker/src/config.js` を開く
   - `APPS_SCRIPT_URL` にコピーしたURLを貼り付け
   
   ```javascript
   export const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/XXXXXXX/exec';
   ```

2. **アプリを再起動**
   - 黒い画面（ターミナル）を閉じて、`run_tracker.bat` を再実行

---

## ✅ 動作確認

1. アプリを開いてログイン画面が表示される
2. 「登録団員: ◯名」と表示されれば成功！
3. 訪問を記録すると、スプレッドシートの「訪問記録」シートに行が追加される

---

## ❓ うまくいかない場合

- **「オフラインモード」と表示される**
  - URLが正しくコピーされているか確認
  - Apps Scriptのアクセス設定が「全員」になっているか確認

- **ログインできない**
  - 「団員リスト」シートのid列に入力した番号と一致しているか確認
  - id列は数字のみ（文字列でもOK）

- **訪問が保存されない**
  - ブラウザのコンソール（F12→Console）でエラーを確認
  - Apps Scriptのログ（実行数→ログを表示）を確認
